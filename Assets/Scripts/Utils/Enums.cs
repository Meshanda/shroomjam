public class Enums
{
    public enum TowerType
    {
        Basic,
        Fast,
        Sniper,
        Support
    }

    public enum GameOverType
    {
        BaseDestroyed, 
        PlayerDead
    };

    public enum PowerType
    {
        Clean,
        Shield,
        Boost
    };
    
    public enum TooltipType
    {
        Default,
        BuildingMenu,
    }
}